import { View, Text, Image } from "react-native";

const PostTypeTag = ({
  type,
}: {
  type: "component" | "workflow" | "knowledge";
}) => {
  return (
    <View>
      <Image />
      <Text></Text>
    </View>
  );
};

export default PostTypeTag;
